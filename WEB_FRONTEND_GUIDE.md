# Web 使用指南

[返回首页](README.md) · [安装与启动](QUICKSTART.md) · [功能范围](WEB_FEATURES.md)

## 1. 添加账户

点击侧栏“添加账户”，填写便于识别的账户名称和超星账号，选择密码或 Cookie Header 验证方式。保存后验证账户，再读取课程。

编辑已保存账户时，留空凭据可以保留原值。账户信息与学习参数保存在本机。

## 2. 配置答题连接

在“全局设置”中填写服务基础地址、模型和 API Key，按需调整超时、重试和答题并发。

**先保存连接，再测试已保存的配置。** 测试成功后，账户才能启动启用了答题的任务。修改连接并再次保存后，要重新测试；仅测试尚未保存的草稿不会更新已保存连接。

服务需兼容 Chat Completions。远程地址例如 `https://api.example.com/v1`；本机服务地址例如 `http://localhost:8849/v1`。这些地址只是格式示例，以自己的服务为准。

全局连接由所有本地账户共享；是否答题、覆盖率和是否自动提交则在各账户的课程工作台中设置。

## 3. 选择课程与学习参数

| 参数 | 用途 |
| --- | --- |
| 课程选择 | 至少选择一门；可搜索、多选、全选或清空 |
| 播放速度 | 1.0–2.0 倍 |
| 章节并发数 | 同时处理的章节数，范围 1–10 |
| 未开放章节 | “稍后重试”或“跳过并继续” |
| 启用答题 | 使用全局设置中的共享答题连接 |
| 答题覆盖率 | 查到答案的比例门槛，取值 0–1 |
| 自动提交答案 | 关闭时保存答案；开启后仍需满足实际提交条件 |

“稍后重试”有重试上限，会等待可能解锁后续章节的任务。“跳过并继续”会处理其余章节，同时保留未完成状态。

题库覆盖率只表示是否查到答案。只有平台返回逐题判定后，才能计算正确率。

## 4. 查看任务结果

任务页将课程、章节、本次读取的任务点分别计数。未开始的直播、未开放章节和失败原因会显示在“需要处理的事项”中。

展开课程可查看每章状态。运行日志保留本次处理过程；页面不会因为网络失败或仅保存答案就把任务计为完成。

需要中断时点击“停止任务”。取消请求会阻止后续处理，已经发送到平台的请求以平台实际接收结果为准。停止后可以返回课程工作台重新开始。

## 5. 查看正确率和成绩

“答题表现”区分本次运行获取的判分与刷新后的所选课程成绩，并显示更新时间。

- 点击“刷新判分”读取当前成绩。该操作不保存答案、不提交测验、不触发重做。
- “未满分与待判分”集中显示仍需关注的测验。
- “全部测验”可查看每份测验的答对题数、分数和状态。
- 部分正确、待判分、未提交和无法读取的题目分别统计。
- 成绩回退时显示此前记录，但不会用旧成绩提高当前正确率。

正确率按完全正确题数除以已判分题数计算。已判分为零时显示暂无结果，不显示虚假的 0% 或 100%。

## 6. 重启与历史记录

服务重启后，账户、参数和任务历史保留。当时仍在运行的任务会标记为中断，重新开始后读取平台已有进度。

历史失败或中断任务不会被后续运行改成成功。平台尚未开放或尚未批阅的内容，也会保留对应状态。

## 接口速查

以下接口由新版 Web 使用，路径均以 `/api` 开头。具体校验与返回结构以 [账户路由](webapp/routes/accounts.py)、[任务路由](webapp/routes/tasks.py) 和 [设置路由](webapp/routes/settings.py) 为准。

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET / POST | `/api/accounts` | 列出或创建账户 |
| GET / PATCH / DELETE | `/api/accounts/{id}` | 读取、编辑或删除账户 |
| POST | `/api/accounts/{id}/verify` | 验证账户 |
| GET | `/api/accounts/{id}/courses?refresh=true` | 刷新课程 |
| GET / PUT / PATCH | `/api/accounts/{id}/preferences` | 读取或保存学习参数 |
| POST | `/api/accounts/{id}/tasks` | 使用课程 ID 列表启动任务 |
| GET | `/api/tasks` | 读取任务历史 |
| GET | `/api/tasks/{id}` | 读取任务状态 |
| GET | `/api/tasks/{id}/details` | 课程树与判分报告 |
| GET | `/api/tasks/{id}/logs?after=0` | 按游标读取日志 |
| POST | `/api/tasks/{id}/cancel` | 请求停止 |
| POST | `/api/tasks/{id}/answer-report` | 启动只读判分刷新 |
| GET / PUT | `/api/settings/answer-connection` | 读取或保存全局答题连接 |
| POST | `/api/settings/answer-connection/test` | 测试连接 |
| GET / PUT | `/api/settings/runtime` | 读取或保存运行限制 |
| GET | `/api/health` | 服务健康检查 |

Web 不提供旧版外部通知面板、多题库选择器或专用 OCR 设置栏。CLI 和后端兼容配置见 [功能对照](FEATURE_COMPARISON.md) 与 [进阶配置](docs/operations.md)。
