# 界面截图

这些图片由当前前端在隔离的只读演示环境中截图生成。账户、课程编号和成绩均为合成数据，没有读取或连接真实账户与答题服务。

- `course-workbench.png`：课程选择与本账户参数。
- `answer-report.png`：正确率、待判分和测验成绩明细。

更新截图时继续使用演示数据，不要上传真实账户页面、API Key、Cookie、个人成绩或带签名参数的链接。
