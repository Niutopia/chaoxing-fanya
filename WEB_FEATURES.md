# 新版 Web 功能说明

[返回首页](README.md) · [使用指南](WEB_FRONTEND_GUIDE.md) · [Web / CLI 对照](FEATURE_COMPARISON.md)

## 页面与功能

| 页面 | 主要内容 |
| --- | --- |
| 概览 | 每个账户最近任务的课程与章节进度、正确率和待处理事项 |
| 课程工作台 | 读取与搜索课程、多选、学习参数、答题开关和任务启动 |
| 任务详情 | 运行状态、完成统计、未完成章节、课程树、判分报告和日志 |
| 全局设置 | 共享答题服务、模型、超时、重试、答题并发与账户运行上限 |

## 账户与课程

- 创建、编辑、验证、启用、停用及删除本地保存的账户。
- 密码或粘贴 Cookie Header 验证；编辑账户时留空凭据可保留原值。
- 读取、刷新、搜索课程，支持多选、全选与清空。
- 课程选择、播放速度、章节并发和未开放策略按账户保存。
- 同一账户同时只能有一个活动任务；至少选择一门课程才能启动。

## 学习与答题

共享引擎处理视频、音频、文档、阅读、直播和章节作业。缺失的必修任务信息、平台拒绝和未开放状态会明确记录。

新版 Web 提供一个共享的 Chat Completions 兼容连接。账户可分别开启答题、设置覆盖率、选择保存或自动提交。

- 实际提交使用平台表单选项值，选项显示字母与提交值可以不同。
- 题目缓存包含选项信息，避免选项顺序变化后复用旧字母。
- 重跑先检查测验页面，已提交的作答会保留；平台提交结果已确认时，判分读取失败不会触发再次提交。
- 覆盖不足、仅保存、关闭答题、作业过期或任务失败不会计为已提交完成。
- 取消会关闭后续提交和进度更新入口；已经发往平台的请求仍需以平台结果为准。

## 正确率与判分报告

**正确率 = 完全正确题数 / 已判分题数。**

部分正确计入已判分数量，但不计入完全正确数量。待判分、未提交和无法读取的题目分别显示，不用满分或覆盖率替代逐题正确率。

“刷新判分”只读取所选课程当前成绩，报告会显示读取范围与更新时间。可以筛选“未满分与待判分”或查看全部测验。

成绩下降、已判分题数减少或测验暂时无法读取时，保留此前记录作为对照；此前数据不会重复计入当前正确率。

## 任务状态与历史

| 状态 | 含义 |
| --- | --- |
| 运行中 | 正在读取或处理任务 |
| 停止中 | 已请求取消，正在结束当前执行过程 |
| 已停止 | 本次运行已取消，可重新开始 |
| 失败 | 存在未完成任务、执行异常或服务重启中断 |
| 已完成 | 本次所选课程的处理结果全部完成 |

课程、章节和本次读取的任务点分别统计。平台已完成章节可能不再展开读取任务点，因此任务点数量不等于课程全部任务的历史总量。

账户、偏好、任务树、日志和判分报告使用 SQLite 持久化。页面每个任务默认保留最近 5000 条日志；详细测验操作记录写入本地诊断日志。旧版已截断的详情或已清理的历史文本会显示说明，无法凭空恢复。

服务重启保留历史，并将当时仍在执行的任务标记为中断。重新启动会读取平台当前进度，旧任务不会自动变回运行中。

## Web 与 CLI 的范围

新版 Web 使用全局共享的 AI 连接，不提供旧版多题库选择器或外部通知设置。其他题库和通知由 CLI 配置。Web 引擎仍兼容读取后端 OCR 配置，目前没有专用 OCR 设置面板。

具体配置入口见 [功能对照](FEATURE_COMPARISON.md)，测试范围见 [验证说明](docs/verification.md)。
